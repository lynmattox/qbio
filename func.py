def function1(a, b, c):
    # I return the result of adding a, b, and c together
    x = a
    y = b
    z = c
    result = x + y + z
    return result

def unpack_me(a, b, c):
    # I return a tuple with 3 elements
    x = a
    y = b
    z = c
    return x, y, z

